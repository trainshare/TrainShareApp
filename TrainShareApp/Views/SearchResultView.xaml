<phone:PhoneApplicationPage
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
    xmlns:vm="clr-namespace:TrainShareApp.ViewModels"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit" 
    x:Class="TrainShareApp.Views.SearchResultView"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True"
    cal:Bind.AtDesignTime="True"
    d:DataContext="{d:DesignInstance IsDesignTimeCreatable=True, Type=vm:SearchResultViewModel}">
	<phone:PhoneApplicationPage.Resources>
		<cal:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
	</phone:PhoneApplicationPage.Resources>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="{StaticResource MainBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Image Grid.Row="0" Stretch="Uniform" Source="/TrainShareApp;component/Logo.png" />

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="1">
            <TextBlock Text="Connections" Style="{StaticResource PhoneTextTitle1Style}" />
            <Grid x:Name="Information" Background="Transparent" Margin="0,30">
            	<Grid.RowDefinitions>
            		<RowDefinition Height="Auto"/>
            		<RowDefinition Height="Auto"/>
            		<RowDefinition Height="Auto"/>
            		<RowDefinition Height="Auto"/>
            	</Grid.RowDefinitions>
            	<Grid.ColumnDefinitions>
            		<ColumnDefinition/>
            		<ColumnDefinition/>
            	</Grid.ColumnDefinitions>
            	<TextBlock HorizontalAlignment="Center" TextWrapping="Wrap" Text="From" Style="{StaticResource PhoneTextSmallStyle}"/>
            	<TextBlock HorizontalAlignment="Center" TextWrapping="Wrap" Text="To" Grid.Column="1" Style="{StaticResource PhoneTextSmallStyle}"/>
            	<TextBlock HorizontalAlignment="Center" TextWrapping="Wrap" Text="{Binding From}" Grid.Row="1" Style="{StaticResource PhoneTextLargeStyle}"/>
            	<TextBlock HorizontalAlignment="Center" TextWrapping="Wrap" Text="{Binding To}" Grid.Row="1" Grid.Column="1" Style="{StaticResource PhoneTextLargeStyle}"/>
            	<TextBlock HorizontalAlignment="Center" TextWrapping="Wrap" Text="Departure" Grid.Row="2" Grid.ColumnSpan="2" Style="{StaticResource PhoneTextSmallStyle}"/>
            	<TextBlock HorizontalAlignment="Center" TextWrapping="Wrap" Text="{Binding Time, Mode=OneWay, StringFormat=HH:mm}" Grid.Row="3" Grid.ColumnSpan="2" Style="{StaticResource PhoneTextLargeStyle}"/>
            </Grid>
        </StackPanel>
        <ListBox x:Name="Results" ItemsSource="{Binding Results}" SelectedItem="{Binding SelectedConnection, Mode=TwoWay}" ItemTemplate="{StaticResource ConnectionTemplate}" Grid.Row="2">
        	<ListBox.ItemContainerStyle>
        		<Style TargetType="ListBoxItem">
        			<Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        		</Style>
        	</ListBox.ItemContainerStyle>
        </ListBox>
    	<toolkit:PerformanceProgressBar Grid.Row="2" IsIndeterminate="{Binding Loading}" Foreground="Black" Margin="0,0,0,300"/>
    </Grid>

</phone:PhoneApplicationPage>
