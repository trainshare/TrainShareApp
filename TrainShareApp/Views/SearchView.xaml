<phone:PhoneApplicationPage
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
    xmlns:vm="clr-namespace:TrainShareApp.ViewModels"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit" 
	mc:Ignorable="d"
	x:Class="TrainShareApp.Views.SearchView"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True"
    cal:Bind.AtDesignTime="True"
    d:DataContext="{d:DesignInstance IsDesignTimeCreatable=True, Type=vm:SearchViewModel}" d:DesignHeight="768" d:DesignWidth="480">

    <Grid x:Name="LayoutRoot" Background="{StaticResource MainBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
		
        <Image Grid.Row="0" Stretch="Uniform" Source="/TrainShareApp;component/Logo.png" />
		<TextBlock Text="Search" Style="{StaticResource PhoneTextTitle1Style}" Grid.Row="1" />
		
		<Grid Grid.Row="2" Height="500" VerticalAlignment="Bottom">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="145" />
				<ColumnDefinition />
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="70" />
				<RowDefinition Height="70" />
				<RowDefinition Height="70" />
				<RowDefinition Height="70" />
				<RowDefinition />
			</Grid.RowDefinitions>
			<TextBlock Text="From" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Style="{StaticResource PhoneTextGroupHeaderStyle}"/>
			<TextBox x:Name="From" Grid.Row="0" Grid.Column="1" Text="{Binding From, Mode=TwoWay}"/>
			<TextBlock Text="To" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Style="{StaticResource PhoneTextGroupHeaderStyle}"/>
			<TextBox x:Name="To" Grid.Row="1" Grid.Column="1" Text="{Binding To, Mode=TwoWay}"/>
			<TextBlock Text="Via" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Style="{StaticResource PhoneTextGroupHeaderStyle}"/>
			<TextBox x:Name="Via" Grid.Row="2" Grid.Column="1" Text="{Binding Via, Mode=TwoWay}"/>
			<TextBlock Text="Time" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Style="{StaticResource PhoneTextGroupHeaderStyle}"/>
			<toolkit:TimePicker Value="{Binding Time, Mode=TwoWay}" Grid.Row="3" Grid.Column="1"/>
			<Button x:Name="SubmitSearch" Grid.Row="4" Grid.Column="1" Content="Go" Width="200" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,48,24"/>
		</Grid>
	</Grid>
</phone:PhoneApplicationPage>