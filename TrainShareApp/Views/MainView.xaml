<phone:PhoneApplicationPage
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
    xmlns:vm="clr-namespace:TrainShareApp.ViewModels"
    xmlns:ctrls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:Views="clr-namespace:TrainShareApp.Views"
    x:Class="TrainShareApp.Views.MainView"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="True"
    cal:Bind.AtDesignTime="True"
    d:DataContext="{d:DesignInstance IsDesignTimeCreatable=True, Type=vm:MainViewModel}">
	<phone:PhoneApplicationPage.Resources>
		<cal:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
	</phone:PhoneApplicationPage.Resources>

    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <cal:AppBarButton Message="Checkin" IconUri="/icons/appbar.check.rest.png" Text="checkin" />
            <cal:AppBarButton Message="Checkout" IconUri="/icons/appbar.cancel.rest.png" Text="checkout" />
            <cal:AppBarButton Message="Settings" IconUri="/icons/appbar.feature.settings.rest.png" Text="settings" />
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="{StaticResource MainBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Image Grid.Row="0" Stretch="Uniform" Source="/TrainShareApp;component/Logo.png" />
        <!--Pivot Control-->
        <controls:Pivot Grid.Row="1">

            <!--Pivot item two-->
            <controls:PivotItem x:Name="CheckinPivot" Header="Checkin">
                <Grid>
					<StackPanel x:Name="InformationWrapper" Margin="12,0" Visibility="{Binding HasCheckedIn, Converter={StaticResource BooleanToVisibilityConverter}}">
						<TextBlock Text="You have currently checked in" Style="{StaticResource PhoneTextTitle2Style}" HorizontalAlignment="Center" Margin="12,0,12,24"/>
                		<Views:TrainInformation DataContext="{Binding CurrentCheckin}" />
                		<Grid>
                			<TextBlock Text="There are currently no friends on this train" Style="{StaticResource PhoneTextSubtleStyle}" VerticalAlignment="Center" Margin="12,120,12,0"/>
                			<ListBox ItemsSource="{Binding Friends}" ItemTemplate="{StaticResource FriendTemplate}" Style="{StaticResource FriendsListStyle}"/>
                		</Grid>
                	</StackPanel>
					<StackPanel Visibility="{Binding HasNotCheckedIn, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}" >
	                	<TextBlock Text="Please check in to see your friends on the same train" TextWrapping="Wrap" Width="300" Style="{StaticResource PhoneTextSubtleStyle}" TextAlignment="Center" Margin="12,120,12,24"/>
						<Button x:Name="Checkin" Content="Checkin" Width="300" Height="100" />
					</StackPanel>
            	</Grid>
            </controls:PivotItem>
            <controls:PivotItem x:Name="HistoryPivot" Header="History">
            	<Grid>
            		<ListBox x:Name="HistoryList" Visibility="{Binding HasHistory, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}" ItemsSource="{Binding History}" ItemTemplate="{StaticResource HistoryTemplate}" HorizontalContentAlignment="Stretch">
			        	<ListBox.ItemContainerStyle>
			        		<Style TargetType="ListBoxItem">
			        			<Setter Property="HorizontalContentAlignment" Value="Stretch"/>
			        		</Style>
			        	</ListBox.ItemContainerStyle>
					</ListBox>
            		<TextBlock Text="You have not checked in until now" Width="305" Visibility="{Binding HasNoHistory, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}" Style="{StaticResource PhoneTextSubtleStyle}" Margin="12,120,12,0"/>
            	</Grid>
            </controls:PivotItem>
        </controls:Pivot>
    </Grid>

</phone:PhoneApplicationPage>