<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">
	<DataTemplate x:Key="ConnectionTemplate">
		<Grid Margin="0,0,0,3" Background="{StaticResource ListItemBrush}" Width="460" Height="60">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition />
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition />
				<RowDefinition />
			</Grid.RowDefinitions>
			<TextBlock x:Name="DepTime" Foreground="Black" Grid.Column="0" Text="{Binding From.Departure, StringFormat=HH:mm}" Grid.Row="1" Style="{StaticResource PhoneTextNormalStyle}"/>
			<TextBlock x:Name="ArrTime" Foreground="Black" Grid.Column="2" Text="{Binding To.Arrival, StringFormat=HH:mm}" Grid.Row="1" Style="{StaticResource PhoneTextNormalStyle}"/>
			<TextBlock x:Name="DepStation" Foreground="Black" Grid.Column="0" Text="{Binding From.Station.Name}" Style="{StaticResource PhoneTextNormalStyle}"/>
			<TextBlock x:Name="ArrStation" Foreground="Black" Grid.Column="2" Text="{Binding To.Station.Name}" Style="{StaticResource PhoneTextNormalStyle}"/>
			<TextBlock x:Name="Changes" Foreground="Black" Text="{Binding Sections.Count, StringFormat='Changes: {0}'}" Grid.RowSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource PhoneTextNormalStyle}" Grid.ColumnSpan="3"/>
		</Grid>
	</DataTemplate>
	<!-- Resource dictionary entries should be defined here. -->
	<DataTemplate x:Key="FriendTemplate">
		<Grid Margin="0,0,0,3" Background="{StaticResource ListItemBrush}" Width="460" Height="90">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="10"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="10"/>
			</Grid.ColumnDefinitions>
			<Image Grid.Column="1" Height="60" Width="60" Margin="0,0,10,0" Source="{Binding ImageUrl}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
			<TextBlock Text="{Binding Name}" FontSize="{StaticResource PhoneFontSizeLarge}" d:LayoutOverrides="Width, Height" Foreground="Black" Grid.Column="2" VerticalAlignment="Center" />
		</Grid>
	</DataTemplate>
	<Style x:Key="FriendsListStyle" TargetType="ListBox">
		<Setter Property="Background" Value="Transparent"/>
		<Setter Property="Foreground" Value="{StaticResource PhoneForegroundBrush}"/>
		<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
		<Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
		<Setter Property="BorderThickness" Value="0"/>
		<Setter Property="BorderBrush" Value="Transparent"/>
		<Setter Property="Padding" Value="0"/>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ListBox">
					<ScrollViewer x:Name="ScrollViewer" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Foreground="{TemplateBinding Foreground}" Padding="{TemplateBinding Padding}" HorizontalContentAlignment="Stretch">
						<ItemsPresenter/>
					</ScrollViewer>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
	<DataTemplate x:Key="HistoryTemplate">
		<Grid Margin="0,0,0,3" Background="{StaticResource ListItemBrush}">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition />
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>
			<TextBlock x:Name="DepTime" Foreground="Black" Text="{Binding DepartureTime, StringFormat=HH:mm}" Grid.Row="1" Grid.Column="1" Style="{StaticResource PhoneTextNormalStyle}" />
			<TextBlock x:Name="DepStation" Foreground="Black" Text="{Binding DepartureStation}" Style="{StaticResource PhoneTextNormalStyle}" />
			<TextBlock x:Name="ArrStation" Foreground="Black" Text="{Binding ArrivalStation}" Style="{StaticResource PhoneTextNormalStyle}" Grid.Row="1" />
		</Grid>
	</DataTemplate>
</ResourceDictionary>